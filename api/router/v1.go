package router

import (
	"MetaFarmBackend/component/context"

	"github.com/gin-gonic/gin"
)

func loadV1(c *gin.Engine, appCtx *context.AppContext) {
	// 加载v1路由
	// apiV1 := c.Group("/api/v1")

	// user := apiV1.Group("/user")
	// {

	// }
}
